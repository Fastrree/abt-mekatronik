/**
 * MAINTENANCE MODE CONFIGURATION
 * 
 * IMPORTANT: Change this value to enable/disable maintenance mode
 * 
 * true  = Site is in maintenance mode (all pages redirect to maintenance page)
 * false = Site is live and accessible
 */
export const MAINTENANCE_MODE = false;

/**
 * HOW TO USE:
 * 
 * 1. To enable maintenance mode:
 *    Change: export const MAINTENANCE_MODE = false;
 *    To:     export const MAINTENANCE_MODE = true;
 * 
 * 2. To disable maintenance mode:
 *    Change: export const MAINTENANCE_MODE = true;
 *    To:     export const MAINTENANCE_MODE = false;
 * 
 * 3. Commit and push changes to deploy
 */
